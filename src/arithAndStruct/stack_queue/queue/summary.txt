队列这个单词是英国人说的排（line）（一种等待服务的方式）。
计算机科学中，队列是一种数据结构，有点类似栈，只是在队列
中第一个插入的数据项也会最先被移除（先进先出，FIFO），而
在栈中，最后插入的数据项最先移除（LIFO）.

有基于数组的队列。也有基于链表的队列。

队列的两个基本操作
一是inserting一个数据项，即把一个数据项放入队尾；
二是removing一个数据项，即移除队头的数据项。

队列的效率：
和栈一样，队列中插入数据和移除数据项的时间复杂度均为O(1).