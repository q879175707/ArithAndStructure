有序链表（sorted_list）可以用与一种搞笑的排序机制。
假设有一个无序数组。如果从这个数组中取出数据。然后一个一个插入有序链表。
它们自动的排好序，把它们从有序表中删除，重新放入数组。数组就排好序了。

这种排序的方式总体上比在数组中常用的插入排序效率要更高些，因为复制的次数少些。
时间级仍然为O(N^2)的过程。

缺点，就是它要开辟差不多两倍的空间：数组和链表必须同时在内存中存在。